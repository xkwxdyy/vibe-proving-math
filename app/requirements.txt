# vibe_proving 依赖
# 安装：pip install -r requirements.txt

# ── 核心 Web 框架 ──────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# ── LLM 客户端 ────────────────────────────────────────────────────────────────
openai>=1.30.0          # AsyncOpenAI，内置 httpx 连接池

# ── HTTP 客户端 ───────────────────────────────────────────────────────────────
httpx>=0.27.0           # 用于 TheoremSearch / LATRACE / arXiv 下载

# ── Harmonic Aristotle（Lean 4 自动形式化 / 证明）────────────────────────────────
aristotlelib>=1.0.1     # https://aristotle.harmonic.fun

# ── 论文解析 ──────────────────────────────────────────────────────────────────
# PyMuPDF (fitz) 是可选依赖：仅在 arXiv PDF 降级路径需要
# 若不需要 PDF 解析，可以注释掉以减少安装体积（~50MB）
PyMuPDF>=1.24.0         # fitz，PDF 解析降级方案

# MinerU PDF 解析增强
ftfy>=6.1.0             # Unicode/Mojibake 修复（pdf_fix 模块使用）
requests>=2.31.0        # MinerU 测试脚本使用（httpx 已覆盖异步路径）

# ── 测试 ──────────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
anyio>=4.0.0

# ── 已移除（原来声明但实际未使用）──────────────────────────────────────────────
# plasTeX>=2.5         # LaTeX 解析库（实际用纯正则，未 import）
# arxiv>=2.1.0         # arXiv Python 库（实际直接用 httpx）
# python-dotenv>=1.0.0 # 环境变量（配置从 TOML 读取，未使用）
